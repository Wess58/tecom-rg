import { NgmodelDebounceChangeDirective } from './ngmodel-debounce-change.directive';

describe('NgmodelDebounceChangeDirective', () => {
  it('should create an instance', () => {
    const directive = new NgmodelDebounceChangeDirective();
    expect(directive).toBeTruthy();
  });
});
