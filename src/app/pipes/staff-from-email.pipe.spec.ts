import { StaffFromEmailPipe } from './staff-from-email.pipe';

describe('StaffFromEmailPipe', () => {
  it('create an instance', () => {
    const pipe = new StaffFromEmailPipe();
    expect(pipe).toBeTruthy();
  });
});
